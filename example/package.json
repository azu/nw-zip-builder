{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "npm rm nw-zip-builder && npm install",
    "test": "nw-zip-builder -p 'win32' -v '0.12.3' ./ && ls"
  },
  "author": "azu",
  "license": "MIT",
  "dependencies": {
    "nw-zip-builder": "file:.."
  }
}
